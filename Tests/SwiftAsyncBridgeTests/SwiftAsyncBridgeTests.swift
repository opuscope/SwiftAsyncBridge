import XCTest
@testable import SwiftAsyncBridge

final class SwiftAsyncBridgeTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftAsyncBridge().text, "Hello, World!")
    }
}
